import type { NextApiRequest, NextApiResponse } from 'next';

export default function handler(req: NextApiRequest, res: NextApiResponse) {
  // Adjust may send data as query params or form data
  const data = { ...req.body, ...req.query };

  // Log or process the callback data
  console.log('Received Adjust callback:', data);

  // TODO: Add your own logic here (e.g., store in DB, trigger workflows, etc.)

  // Respond with 200 OK
  res.status(200).send('OK');
}

// Optional: To allow POST requests with form data
export const config = {
  api: {
    bodyParser: {
      sizeLimit: '1mb',
    },
  },
}; 
